services:
  mysqldb:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: expensedb
    ports:
      - "3307:3306"
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - mysqldb
    restart: on-failure